<?  	
	
	/*
include_once $path_forum."/config.php";	
	
	$db_host = $dbhost;
	$db_user = $dbuser;
	$db_name = $dbname;
	$db_pass = $dbpasswd;
	$db_port = $dbport;

	//echo $dbhost."__".$dbname."__".$db_user."__".$dbpasswd."__".$dbhost."__<br>";
		
	$sql_db = "dbal_mysqli";
	$db	= new $sql_db();

	// Connect to DB
	$db->sql_connect($dbhost, $dbuser, $dbpasswd, $dbname, $dbport, false, defined('PHPBB_DB_NEW_LINK') ? PHPBB_DB_NEW_LINK : false);
	
	/*
	 
	$db->sql_query("SET character_set_client=cp1251");
	$db->sql_query("SET character_set_connection=cp1251");
	$db->sql_query("SET character_set_results=cp1251");	
	*/
	
	//$link = mysql_connect($db_host, $db_user, $db_pass)
		//or die("Could not connect.");		
	//mysql_select_db($db_name) or die("Could not select database $db_name");
	//$db->sql_query("SET character_set_client=cp1251",$link);
	//$db->sql_query("SET character_set_connection=cp1251",$link);
	//$db->sql_query("SET character_set_results=cp1251",$link);

?>